// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/quii/go-fakes-and-contracts/adapters/driven/persistence/sqlite/ent/ingredient"
	"github.com/quii/go-fakes-and-contracts/adapters/driven/persistence/sqlite/ent/pantry"
)

// PantryCreate is the builder for creating a Pantry entity.
type PantryCreate struct {
	config
	mutation *PantryMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetQuantity sets the "quantity" field.
func (pc *PantryCreate) SetQuantity(i int) *PantryCreate {
	pc.mutation.SetQuantity(i)
	return pc
}

// SetIngredientID sets the "ingredient" edge to the Ingredient entity by ID.
func (pc *PantryCreate) SetIngredientID(id int) *PantryCreate {
	pc.mutation.SetIngredientID(id)
	return pc
}

// SetNillableIngredientID sets the "ingredient" edge to the Ingredient entity by ID if the given value is not nil.
func (pc *PantryCreate) SetNillableIngredientID(id *int) *PantryCreate {
	if id != nil {
		pc = pc.SetIngredientID(*id)
	}
	return pc
}

// SetIngredient sets the "ingredient" edge to the Ingredient entity.
func (pc *PantryCreate) SetIngredient(i *Ingredient) *PantryCreate {
	return pc.SetIngredientID(i.ID)
}

// Mutation returns the PantryMutation object of the builder.
func (pc *PantryCreate) Mutation() *PantryMutation {
	return pc.mutation
}

// Save creates the Pantry in the database.
func (pc *PantryCreate) Save(ctx context.Context) (*Pantry, error) {
	return withHooks(ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PantryCreate) SaveX(ctx context.Context) *Pantry {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PantryCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PantryCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PantryCreate) check() error {
	if _, ok := pc.mutation.Quantity(); !ok {
		return &ValidationError{Name: "quantity", err: errors.New(`ent: missing required field "Pantry.quantity"`)}
	}
	return nil
}

func (pc *PantryCreate) sqlSave(ctx context.Context) (*Pantry, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *PantryCreate) createSpec() (*Pantry, *sqlgraph.CreateSpec) {
	var (
		_node = &Pantry{config: pc.config}
		_spec = sqlgraph.NewCreateSpec(pantry.Table, sqlgraph.NewFieldSpec(pantry.FieldID, field.TypeInt))
	)
	_spec.OnConflict = pc.conflict
	if value, ok := pc.mutation.Quantity(); ok {
		_spec.SetField(pantry.FieldQuantity, field.TypeInt, value)
		_node.Quantity = value
	}
	if nodes := pc.mutation.IngredientIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: false,
			Table:   pantry.IngredientTable,
			Columns: []string{pantry.IngredientColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(ingredient.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Pantry.Create().
//		SetQuantity(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.PantryUpsert) {
//			SetQuantity(v+v).
//		}).
//		Exec(ctx)
func (pc *PantryCreate) OnConflict(opts ...sql.ConflictOption) *PantryUpsertOne {
	pc.conflict = opts
	return &PantryUpsertOne{
		create: pc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Pantry.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (pc *PantryCreate) OnConflictColumns(columns ...string) *PantryUpsertOne {
	pc.conflict = append(pc.conflict, sql.ConflictColumns(columns...))
	return &PantryUpsertOne{
		create: pc,
	}
}

type (
	// PantryUpsertOne is the builder for "upsert"-ing
	//  one Pantry node.
	PantryUpsertOne struct {
		create *PantryCreate
	}

	// PantryUpsert is the "OnConflict" setter.
	PantryUpsert struct {
		*sql.UpdateSet
	}
)

// SetQuantity sets the "quantity" field.
func (u *PantryUpsert) SetQuantity(v int) *PantryUpsert {
	u.Set(pantry.FieldQuantity, v)
	return u
}

// UpdateQuantity sets the "quantity" field to the value that was provided on create.
func (u *PantryUpsert) UpdateQuantity() *PantryUpsert {
	u.SetExcluded(pantry.FieldQuantity)
	return u
}

// AddQuantity adds v to the "quantity" field.
func (u *PantryUpsert) AddQuantity(v int) *PantryUpsert {
	u.Add(pantry.FieldQuantity, v)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create.
// Using this option is equivalent to using:
//
//	client.Pantry.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//		).
//		Exec(ctx)
func (u *PantryUpsertOne) UpdateNewValues() *PantryUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Pantry.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *PantryUpsertOne) Ignore() *PantryUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *PantryUpsertOne) DoNothing() *PantryUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the PantryCreate.OnConflict
// documentation for more info.
func (u *PantryUpsertOne) Update(set func(*PantryUpsert)) *PantryUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&PantryUpsert{UpdateSet: update})
	}))
	return u
}

// SetQuantity sets the "quantity" field.
func (u *PantryUpsertOne) SetQuantity(v int) *PantryUpsertOne {
	return u.Update(func(s *PantryUpsert) {
		s.SetQuantity(v)
	})
}

// AddQuantity adds v to the "quantity" field.
func (u *PantryUpsertOne) AddQuantity(v int) *PantryUpsertOne {
	return u.Update(func(s *PantryUpsert) {
		s.AddQuantity(v)
	})
}

// UpdateQuantity sets the "quantity" field to the value that was provided on create.
func (u *PantryUpsertOne) UpdateQuantity() *PantryUpsertOne {
	return u.Update(func(s *PantryUpsert) {
		s.UpdateQuantity()
	})
}

// Exec executes the query.
func (u *PantryUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for PantryCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *PantryUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *PantryUpsertOne) ID(ctx context.Context) (id int, err error) {
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *PantryUpsertOne) IDX(ctx context.Context) int {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// PantryCreateBulk is the builder for creating many Pantry entities in bulk.
type PantryCreateBulk struct {
	config
	builders []*PantryCreate
	conflict []sql.ConflictOption
}

// Save creates the Pantry entities in the database.
func (pcb *PantryCreateBulk) Save(ctx context.Context) ([]*Pantry, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Pantry, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PantryMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = pcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PantryCreateBulk) SaveX(ctx context.Context) []*Pantry {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PantryCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PantryCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Pantry.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.PantryUpsert) {
//			SetQuantity(v+v).
//		}).
//		Exec(ctx)
func (pcb *PantryCreateBulk) OnConflict(opts ...sql.ConflictOption) *PantryUpsertBulk {
	pcb.conflict = opts
	return &PantryUpsertBulk{
		create: pcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Pantry.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (pcb *PantryCreateBulk) OnConflictColumns(columns ...string) *PantryUpsertBulk {
	pcb.conflict = append(pcb.conflict, sql.ConflictColumns(columns...))
	return &PantryUpsertBulk{
		create: pcb,
	}
}

// PantryUpsertBulk is the builder for "upsert"-ing
// a bulk of Pantry nodes.
type PantryUpsertBulk struct {
	create *PantryCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.Pantry.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//		).
//		Exec(ctx)
func (u *PantryUpsertBulk) UpdateNewValues() *PantryUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Pantry.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *PantryUpsertBulk) Ignore() *PantryUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *PantryUpsertBulk) DoNothing() *PantryUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the PantryCreateBulk.OnConflict
// documentation for more info.
func (u *PantryUpsertBulk) Update(set func(*PantryUpsert)) *PantryUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&PantryUpsert{UpdateSet: update})
	}))
	return u
}

// SetQuantity sets the "quantity" field.
func (u *PantryUpsertBulk) SetQuantity(v int) *PantryUpsertBulk {
	return u.Update(func(s *PantryUpsert) {
		s.SetQuantity(v)
	})
}

// AddQuantity adds v to the "quantity" field.
func (u *PantryUpsertBulk) AddQuantity(v int) *PantryUpsertBulk {
	return u.Update(func(s *PantryUpsert) {
		s.AddQuantity(v)
	})
}

// UpdateQuantity sets the "quantity" field to the value that was provided on create.
func (u *PantryUpsertBulk) UpdateQuantity() *PantryUpsertBulk {
	return u.Update(func(s *PantryUpsert) {
		s.UpdateQuantity()
	})
}

// Exec executes the query.
func (u *PantryUpsertBulk) Exec(ctx context.Context) error {
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the PantryCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for PantryCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *PantryUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
